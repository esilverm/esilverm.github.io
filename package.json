{
  "name": "esilverm.github.io",
  "version": "1.0.0",
  "description": "This is my planning space for my website. I haven't worked on something like this in a while and I realize I probably should.",
  "main": "index.js",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "rm -rf node_modules/.cache && next build && next export && touch out/.nojekyll && git add out/ && git commit -m \"Deploy Next.js to gh-pages\" && git subtree push --prefix out origin gh-pages"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/esilverm/esilverm.github.io.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
   "babel-plugin-transform-define": "^1.3.0"
  },
  "bugs": {
    "url": "https://github.com/esilverm/esilverm.github.io/issues"
  },
  "homepage": "https://github.com/esilverm/esilverm.github.io#readme",
  "dependencies": {
    "next": "^9.1.2",
    "react": "^16.11.0",
    "react-dom": "^16.11.0"
  }
}
